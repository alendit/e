#+title: Plan: Context Compaction
#+author: e planning
#+date: 2026-05-25
#+startup: overview

* Goal
Add append-only context compaction for long =e= sessions.

The first version should let a user manually compact the current session so future
turns send a structured summary plus a recent verbatim suffix instead of the full
transcript.

This feature depends on feature 04, =Message and Turn Identity=. Compaction
boundaries must be stored by durable entry id, not by list index, buffer
position, or message count.

The durable session history remains intact. Compaction changes context
construction, not historical storage.

After compaction, context construction should send:

#+begin_example
system/default context
compaction summary message
kept recent transcript entries from first-kept-entry-id to current head
new entries after the compaction record
#+end_example

* Non-goals
- Implement feature 04 identities; this plan assumes that work exists.
- Implement full branching UI or branch summarization.
- Implement automatic threshold-based compaction.
- Implement context-overflow recovery and automatic retry.
- Implement custom extension hooks for compaction.
- Replace the whole context system with a speculative strategy framework.
- Destructively delete, rewrite, or redact historical session entries.
- Perfect token accounting across all providers.

* Dependencies
This feature should start after feature 04 supplies at least:

- durable =id= for session entries;
- =parent-id= and current path reconstruction;
- =turn-id= for user/assistant/tool entries belonging to one operation;
- query entry by id;
- query latest entry of a type on the current path;
- query suffix from a boundary id to current head;
- enough turn/tool grouping information to avoid unsafe cut points.

Feature 10, =Session Append Performance=, is not strictly required but is a good
predecessor if compaction adds more durable entry types or long-session tests.

* Design summary
Compaction is a session/context policy, not transcript mutation.

The session store owns durable compaction records. The context layer owns how a
session path is converted into backend messages. The harness or chat shell owns
user-facing initiation, progress reporting, and summary generation orchestration.

High-level components:

| Component | Responsibility |
|-----------+----------------|
| =e-session= | Store/load compaction records and expose identity-based queries. |
| =e-context= | Build compacted backend context from latest compaction plus kept suffix. |
| Harness service | Prepare compaction, call summarizer backend, append record, refresh live context. |
| Chat shell | Provide manual command/action and render progress/errors. |

Keep side effects at the edges:

- session writes happen through =e-session=;
- backend calls happen through harness/backend APIs;
- chat UI only starts the operation and displays status.

* Durable compaction record
Feature 04 already sketches a compaction entry. This feature should make it real.

Required fields:

| Field | Meaning |
|-------+---------|
| =id= | durable compaction entry id |
| =parent-id= | previous head at time compaction was appended |
| =type= | =compaction= |
| =summary= | structured model-generated summary |
| =first-kept-entry-id= | oldest current-path entry replayed verbatim after summary |
| =tokens-before= | estimated or measured context size before compaction |
| =created-at= | creation timestamp |
| =metadata= | reason, instructions, active model, resource usage metadata, estimates |

Suggested record shape:

#+begin_src elisp
(:id "01J..."
 :parent-id "01J..."
 :type compaction
 :summary "..."
 :first-kept-entry-id "01J..."
 :tokens-before 123456
 :created-at "2026-05-25T08:00:00Z"
 :metadata (:reason manual
            :instructions "focus on ..."
            :summary-model "gpt-..."
            :previous-compaction-id "01J..."
            :affected-resources
            ((:uri "file://lisp/core/e-context.el"
              :operation read
              :tools ("read"))
             (:uri "tmp://test-output/turn-01.txt"
              :operation read
              :tools ("bash")))
            :tool-usage-summaries
            ((:tool "bash"
              :kind resource-usage
              :resources ((:uri "tmp://test-output/turn-01.txt"
                           :operation read))
              :summary "kept full test output in tmp resource"))
            :read-files ("lisp/core/e-context.el") ; derived compatibility view
            :modified-files nil                 ; derived compatibility view
            :messages-summarized 42
            :turns-summarized 12
            :estimated-tokens-kept 18000))
#+end_src

Do not expose these ids as ordinary user-facing content. They are storage and
debug metadata.

* Context reconstruction
Add a compacted context strategy or extend the transcript strategy with a clearly
owned compaction branch.

For the current path:

1. Find the latest =compaction= entry on the current path.
2. If none exists, use the normal transcript strategy.
3. If one exists, validate =first-kept-entry-id= is on the current path.
4. Emit a synthetic summary message derived from the compaction record.
5. Emit model-facing entries from =first-kept-entry-id= to the current head,
   excluding storage-only entries and the compaction record itself unless needed.

Follow Pi's shape for the compacted context item: emit one explicit
provider-neutral compaction summary message before the kept suffix. In =e= terms,
the context layer should emit an internal message with =:role compaction-summary=
and content derived from the durable summary:

#+begin_src elisp
(:role compaction-summary
 :content "Previous session context was compacted. Summary:\n\n...")
#+end_src

Backend normalization lowers this internal role to the provider-facing shape. The
initial backend form can be a =system= message because it is not a user request
and should not be treated as dialogue to answer:

#+begin_src elisp
(:role system
 :content "Previous session context was compacted. Summary:\n\n...")
#+end_src

Keep this choice owned by the context/backend normalization layer so provider
adapters can map it differently later if needed. The =compaction-summary= role is
not durable session data; it is an internal context item used between session
reconstruction and backend normalization. Storage-only fields such as =:id=,
=:parent-id=, =:turn-id=, and =:first-kept-entry-id= must not leak into backend
messages.

* Manual compaction flow
The first shipped trigger should be manual only.

Suggested command names:

- =e-chat-compact-session=
- optional prompt argument for custom instructions

Flow:

1. User invokes manual compaction from a chat session.
2. If a turn is running, refuse with a clear message. Do not auto-abort the turn
   and do not compact concurrently with an active turn in the first version.
3. Build a compaction preparation from the current path.
4. Choose a kept boundary.
5. Serialize older entries into a summarization transcript.
6. Call the configured summarizer backend.
7. Append a durable compaction record.
8. Rebuild live harness context from the compacted context strategy.
9. Render a compact success/progress block in chat.

Manual compaction errors should be expected, visible errors:

- no active session;
- session has too little context to compact;
- no safe cut point;
- summarizer request failed;
- compaction boundary no longer valid because the session changed.

* Boundary selection
Use a simple first version: keep approximately a configurable number of recent
estimated tokens, preserving turn/tool integrity.

Suggested defcustoms:

#+begin_src elisp
(defcustom e-context-compaction-keep-recent-tokens 20000
  "Approximate recent transcript tokens to keep verbatim after compaction."
  :type 'integer)

(defcustom e-context-compaction-tool-result-summary-limit 2000
  "Maximum characters from one tool result in summarization input."
  :type 'integer)
#+end_src

Boundary selection should:

- walk current-path entries from newest to oldest;
- accumulate rough token estimates;
- select the oldest safe entry that keeps around
  =e-context-compaction-keep-recent-tokens=;
- prefer turn boundaries where possible;
- never separate a tool result from the corresponding tool call;
- never choose a boundary before an entry that cannot be rendered to backend
  context safely.

For a first target model with roughly 256k tokens of context, keep a conservative
recent suffix of about 20k tokens. This matches Pi's documented default and keeps
around 8% of the window verbatim while leaving room for default/system context,
tools, generated summary, and the next answer. Codex exposes manual thread
compaction but the public docs do not document a keep-recent token constant, so
use Pi's value rather than inventing a Codex-derived number.

If the current turn alone exceeds the keep budget, the first implementation may
refuse compaction with “current turn is too large to compact safely”. Split-turn
compaction can be a follow-up.

* Summarization input
Do not feed old messages to the summarizer as a live chat to continue. Convert
entries into a plain transcript.

Example serialization:

#+begin_example
[User]: What the user asked
[Assistant]: Response text
[Assistant tool calls]: read(uri="file://...")
[Tool result]: First 2000 chars of result...
[Error]: Tool failed: ...
#+end_example

Rules:

- include enough role/type labels to make provenance clear;
- truncate large tool results in summarization input;
- include previous compaction summary when updating a rolling summary;
- include optional user instructions as summarizer focus, not as transcript
  content;
- avoid including storage-only ids unless needed for internal diagnostics.

* Summary format
Use a structured summary format close to =docs/research/context_compaction.org=:

#+begin_src markdown
## Goal
[What the user is trying to accomplish]

## Constraints & Preferences
- [User requirements, style preferences, safety constraints]

## Progress
### Done
- [x] [Completed work]

### In Progress
- [ ] [Current work]

### Blocked
- [Issues, if any]

## Key Decisions
- **[Decision]**: [Rationale]

## Next Steps
1. [Most likely next action]

## Critical Context
- [Facts needed to continue]

<affected-resources>
Read:
- file://path/to/file
- tmp://artifact

Write:
- file://path/to/changed-file
</affected-resources>

<read-files>
path/to/file
</read-files>

<modified-files>
path/to/changed-file
</modified-files>
#+end_src

The =affected-resources= block is the primary resource-oriented view. Keep
=read-files= and =modified-files= as derived compatibility tags only when file
URIs are available.

For repeated compaction, pass the previous summary and ask the summarizer to
update it while preserving still-relevant information. Do not stack multiple
summary messages in context; use the latest compaction on the current path.

* Tool usage and affected-resource metadata
The first version should not hard-code compaction metadata as only
=read-files=/=modified-files=. =e= tools operate on resources, buffers, tmp
artifacts, Emacs state, shell commands, and external CLIs.

Use one generic, resource-centric usage metadata contract for both resource
methods and free tools. Compaction consumes this metadata to build the durable
resource list and to give the summarizer compact, high-value tool context. This
is part of the tool result/turn metadata contract, not a compaction-specific
lifecycle hook.

Store usage records on durable tool-result/session-entry metadata under the
=:tool-usage= key. The value should be a list of usage records, even when a tool
reports only one record. This keeps the carrier generic while allowing later
non-resource usage kinds without changing the surrounding entry shape.

Resource methods report usage implicitly. For example, =read=, =write=, and
=edit= know the target URI and operation, so callers do not need to opt in for
those records. Free tools may accept optional usage metadata supplied by the LLM
when the call touches resources that should survive compaction. The guidance for
free tools should be short and resource-centric: if this call affects resources
that matter for future work, provide the resource URIs and operations; otherwise
omit the metadata. Do not add noisy usage records for every shell command.

Canonical metadata shape:

#+begin_src elisp
(:metadata
 (:tool-usage
  ((:kind resource-usage
    :tool "bash"
    :resources ((:uri "file://lisp/core/e-context.el"
                 :operation read)
                (:uri "tmp://test-output/turn-01.txt"
                 :operation write))
    :summary "ran targeted context tests and stored full output in tmp resource"))))
#+end_src

Resource method calls can produce the same shape without an explicit LLM
argument:

#+begin_src elisp
(:metadata
 (:tool-usage
  ((:kind resource-usage
    :tool "read"
    :resources ((:uri "file://lisp/core/e-context.el"
                 :operation read))))))
#+end_src

Compaction preparation should normalize these tool usage records into durable
metadata:

#+begin_src elisp
(:affected-resources
 ((:uri "file://lisp/core/e-context.el"
   :operation read
   :tools ("read" "bash"))
  (:uri "tmp://test-output/turn-01.txt"
   :operation write
   :tools ("bash")))
 :tool-usage-summaries
 ((:tool "bash"
   :kind resource-usage
   :resources ((:uri "file://lisp/core/e-context.el" :operation read)
               (:uri "tmp://test-output/turn-01.txt" :operation write))
   :summary "ran targeted context tests and stored full output in tmp resource")))
#+end_src

Rules:

- store usage on tool result/session-entry metadata as =:tool-usage=, whose
  value is a list of records;
- require =:kind= and keep it visible so later usage metadata can distinguish
  resource usage from other kinds without changing the carrier contract;
- use =resource-usage= as the first supported kind;
- keep resource entries URI-based and operation-oriented;
- deduplicate resources by URI and operation when building
  =:affected-resources=;
- keep optional summaries bounded and high-value; they are not a place to store
  raw command output or duplicate the transcript;
- do not try to infer every file a free tool may have touched;
- when a tool stores truncated output in a =tmp://= resource, include that URI as
  an affected resource;
- derive legacy-friendly =read-files= and =modified-files= for generated
  markdown only when file URIs are available, but keep the durable metadata
  resource-oriented.

* Backend and model choice
Use the active default backend/model for the first version. Do not add a summary
model setting in this feature.

A later feature can add a dedicated summary model/provider if active-model
summaries are too expensive or too weak. Do not introduce a broad summarizer
abstraction until there are at least two real summary-generation implementations
or a concrete provider-specific need.

* UI and activity events
The chat shell should expose a manual compaction command and report lifecycle
status.

Suggested activity events:

- =compaction-started=;
- =compaction-prepared=;
- =compaction-summary-started=;
- =compaction-finished=;
- =compaction-failed=.

The UI should show a compact block with:

- reason: manual;
- summary model, if known from the active backend;
- summarized turns/messages;
- kept boundary timestamp or brief label;
- token estimates before/kept, when known.

Avoid dumping the entire generated summary into a noisy activity block if the
summary is already stored and used by context. Provide a way to inspect it later
through session/debug tooling.

* Implementation steps
** 1. Add compaction storage shape
Extend or replace the existing simple compaction persistence to include feature
04 identity fields and =first-kept-entry-id=.

Acceptance criteria:

- compaction records round-trip through JSONL;
- compaction records appear on the current path like other durable entries;
- legacy simple compaction records, if any, still load or are ignored safely.

** 2. Add compaction query helpers
Add helpers for:

- latest compaction on current path;
- validate boundary id is on current path;
- entries from boundary id to current head;
- entries before boundary id to summarize;
- count summarized/kept entries.

Acceptance criteria:

- helpers are core/session-level, independent of chat buffers;
- tests cover no compaction, one compaction, repeated compaction, and synthetic
  branch paths when feature 04 supports them.

** 3. Add compacted context reconstruction
Teach the context layer to build backend messages from latest compaction plus
kept suffix.

Acceptance criteria:

- no compaction produces the same messages as transcript context;
- with compaction, reconstructed context includes one internal
  =compaction-summary= message plus kept suffix;
- backend normalization lowers =compaction-summary= to the provider-facing
  summary representation, initially a =system= message;
- messages before =first-kept-entry-id= are not sent verbatim;
- storage-only identity metadata is stripped before backend submission.

** 4. Add preparation and boundary selection
Create a pure preparation function that inspects the current path and returns:

#+begin_src elisp
(:previous-compaction ...
 :first-kept-entry-id "01J..."
 :messages-to-summarize (...)
 :kept-entries (...)
 :tokens-before 12345
 :tokens-kept 6789
 :affected-resources (...)
 :tool-usage-summaries (...))
#+end_src

Acceptance criteria:

- preparation has no backend calls and no session writes;
- it rejects sessions with no safe cut point;
- it avoids unsafe tool-call/tool-result cuts;
- tests cover normal and too-small sessions.

** 5. Add generic tool usage metadata
Add a generic resource-centric usage metadata carrier to tool result/session
entry metadata. Resource methods should populate it implicitly from their URI and
operation. Free tools should expose an optional argument for high-value resource
usage only and translate that argument into the same =:metadata (:tool-usage
...)= carrier.

Acceptance criteria:

- resource methods emit =resource-usage= metadata with URI and operation under
  =:metadata (:tool-usage ...)=;
- free tools can accept optional resource usage metadata without requiring it for
  every call;
- compaction preparation deduplicates resource usage by URI and operation;
- summaries are bounded and raw payloads are not duplicated in metadata;
- tests cover implicit resource-method metadata and optional free-tool metadata.

** 6. Add transcript serialization for summarization
Serialize entries to a plain text summarization input with role/type labels,
truncated tool outputs, and compact method-centric resource summaries.

Acceptance criteria:

- user, assistant, tool-call, tool-result, and error entries are distinguishable;
- tool result truncation is tested;
- serialized text does not include large unbounded payloads;
- resource usage is summarized by operation, for example =Read:= and =Write:=
  lists with deduplicated URIs.

** 7. Add summary generation service
Add a small harness-owned operation that calls the active backend with a
summarizer system prompt and serialized transcript.

Acceptance criteria:

- summary prompt requests only the structured summary;
- optional manual instructions are included;
- previous summary is included for repeated compaction;
- backend failures surface as visible compaction errors.

** 8. Add manual chat command
Expose manual compaction from chat.

Acceptance criteria:

- command works for the active chat session;
- optional instructions can be supplied interactively or via prefix/minibuffer;
- command refuses to run concurrently with an active assistant turn;
- success appends a compaction record and refreshes live context.

** 9. Add activity/rendering support
Record and render compact lifecycle events.

Acceptance criteria:

- start/success/failure are visible in chat;
- durable activity events replay sensibly;
- failures do not leave partially appended compaction records.

** 10. Add tests and live validation
Add tests for storage, pure preparation, context reconstruction, serialization,
and command orchestration with a fake backend.

Acceptance criteria:

- context behavior is proven without a real LLM call;
- fake backend test proves manual compaction appends a record;
- live Emacs is reloaded after Lisp changes and a temporary chat/session scenario
  is inspected when feasible.

* Testing plan
Run targeted tests at minimum:

#+begin_example
eldev test test/e-session-test.el test/e-context-test.el test/e-harness-test.el test/e-chat-test.el
#+end_example

Add or extend tests for:

- compaction record persistence/replay;
- latest compaction lookup;
- boundary suffix reconstruction;
- compacted context emits summary plus kept suffix;
- ids and turn ids are stripped from backend messages;
- no-compaction context remains unchanged;
- preparation rejects unsafe or too-small sessions;
- summarization serialization truncates tool outputs;
- repeated compaction uses previous summary;
- manual command with fake backend appends compaction and reports lifecycle.

For behavior changes in loaded Lisp, reload the running Emacs with:

#+begin_example
emacsclient --eval "(progn (require 'e-dev) (e-dev-reload \"/Users/dimitrivorona/projects/elisp/e\"))"
#+end_example

Then inspect a live temporary session where possible.

* Migration and compatibility
Existing sessions may contain either no compaction records or older simple
compaction records with only =summary=, =branch-id=, =range=, and =metadata=.

Policy:

- do not rewrite old session files automatically;
- old simple records may be exposed for inspection but should not drive compacted
  context unless they have a valid =first-kept-entry-id=;
- sessions without compaction records continue using transcript context;
- feature 04 legacy id backfill should provide in-memory ids for old entries so a
  newly appended compaction can reference a valid boundary going forward.

* Risks
- If ids leak into backend messages, compaction will increase token use and may
  confuse the model. Strip storage metadata in one owned normalization path.
- If boundary selection cuts inside a tool interaction, future turns may see
  invalid context. Prefer conservative rejection over unsafe compaction.
- If compaction runs during an active turn, the summary may describe a moving
  target. The first version should reject concurrent compaction.
- Summary quality depends on the active model. A bad summary can lose important
  context even though storage remains intact.
- Repeated compaction can accidentally drop older decisions if the update prompt
  is weak. Tests can verify plumbing, but prompt quality needs manual review.
- Branching later may require careful definition of “latest compaction on current
  path”; avoid global latest-by-time lookups.

* Follow-up work
- Auto-compaction based on context usage thresholds.
- Context-overflow recovery with one retry.
- Split-turn compaction for huge turns.
- Branch summarization during future tree navigation.
- Custom compaction strategy hooks.
- Dedicated summary model/provider configuration if active-model summaries are
  too expensive or too weak.
- Additional usage metadata kinds if concrete non-resource use cases emerge.
- UI for inspecting compaction summaries and boundaries.
- Commands to discard a compaction record from the current path by branching or
  checkout semantics, once branching exists.

* Resolved planning decisions
- Summary message role :: Follow Pi's shape conceptually: a distinct compaction
  summary item before the kept suffix. Represent it in =e= as an internal
  =compaction-summary= role emitted by context reconstruction, and initially
  normalize it to a backend =system= message.
- Running turns :: Manual compaction refuses to run while a turn is active. It
  does not auto-abort.
- Kept context size :: Default =e-context-compaction-keep-recent-tokens= to
  about 20k tokens for the first version. This follows Pi's documented default;
  Codex public docs expose compaction endpoints but do not document a keep-recent
  constant.
- Summary model :: Use the active default model/backend. Dedicated summary-model
  configuration is deferred.
- Legacy simple compaction display :: Defer. Old simple records should not drive
  compacted context unless they have a valid boundary.
- Metadata model :: Prefer durable resource-centric =affected-resources= and
  =tool-usage-summaries= over file-only metadata. Tool result/session-entry
  metadata carries a =:tool-usage= list of usage records. Resource methods emit
  usage metadata implicitly; free tools can optionally report high-value affected
  resources. Derive =read-files= and =modified-files= from resource metadata only
  as a markdown compatibility view.
- Affected-resource reporting :: Add a generic tool usage metadata carrier now,
  not a compaction-specific lifecycle hook. The first supported kind is
  =resource-usage=. Compaction consumes these records, deduplicates resources by
  URI and operation, and emits method-centric summaries such as =Read:= and
  =Write:= lists.

* Open questions
No open questions remain for the first manual compaction feature. The former open
questions were resolved as concrete plan commitments:

- Internal summary role :: Use =compaction-summary= as the internal context role.
  Backend normalization lowers it to the provider-facing summary representation,
  initially =system=.
- Running turns :: Manual compaction refuses while a turn is active; it does not
  auto-abort.
- Kept context size :: Default =e-context-compaction-keep-recent-tokens= to about
  20k tokens.
- Summary model :: Use the active default backend/model; defer dedicated summary
  model configuration.
- Legacy simple records :: Old records may be inspectable, but do not drive
  compacted context unless they have a valid =first-kept-entry-id=.
- Affected-resource reporting :: Add generic resource-centric tool usage metadata
  now. Store usage records under =:metadata (:tool-usage ...)= on tool
  result/session entries; do not add a compaction-specific lifecycle hook.

* Minimal acceptance for this feature
The feature is complete when:

1. Manual compaction can append an identity-bearing compaction record with a
   valid =first-kept-entry-id=.
2. Context construction uses the latest valid compaction on the current path to
   send summary plus kept suffix.
3. Historical transcript entries remain persisted and are not destructively
   removed.
4. Repeated manual compaction updates from the previous summary instead of
   stacking multiple summaries in backend context.
5. Boundary selection is conservative and does not knowingly split tool-call /
   tool-result groups.
6. Large tool outputs are truncated in summarization input.
7. Resource usage metadata is captured for resource methods, can be provided by
   free tools for high-value affected resources, and is deduplicated for
   compaction summaries.
8. Expected compaction failures are visible and do not corrupt session state.
9. Storage, preparation, context reconstruction, tool usage metadata,
   serialization, and manual orchestration are covered by tests.
