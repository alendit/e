#+title: Session Overview Shell Review
#+date: 2026-05-26

* Status

In-review.

The implementation is present in the current tree and matches the ready plan.
This feature remains =In-review= until the operator accepts the review round.

* Implementation Summary

- =e-chat-switch-session= preserves the existing resume behavior by delegating
  to =e-chat-resume=.
- =e-chat-overview= opens a side-window overview buffer listing persisted chat
  sessions in the order returned by =e-harness-session-list=.
- Overview rows show styled title, unread marker, compact metadata, compact
  timestamp, and subdued summary text instead of a dense raw text listing.
  Summary text is suppressed when it would only repeat a derived title.
- Sidebar =j= and =k= move between session rows, not physical lines, and moving
  to a row opens the session preview.
- Overview read markers are presentation-owned JSON state, stored in the active
  session store directory by default.
- Opening a session from the overview reuses =e-chat-open= and records the latest
  assistant message marker as read.
- The overview subscribes to harness events and refreshes while its buffer is
  live.
- The chat shell manifest advertises =switch-session=, =overview=, and
  =overview-close=.

* Verification

Ran:

#+begin_src sh
rtk eldev test -f test/e-chat-test.el e-chat-test-overview-renders-sessions-in-recency-order e-chat-test-overview-open-session-marks-session-read e-chat-test-shell-descriptor-advertises-chat-surface
rtk eldev test -f test/e-chat-test.el e-chat-test-overview-renders-sessions-in-recency-order e-chat-test-overview-compacts-multiline-session-summary e-chat-test-overview-styles-session-row-regions e-chat-test-overview-hides-summary-when-title-is-derived e-chat-test-overview-j-k-move-by-session-and-preview e-chat-test-overview-open-session-marks-session-read e-chat-test-sidebar-toggle-opens-and-closes-overview e-chat-test-shell-descriptor-advertises-chat-surface
rtk eldev test -f test/e-chat-test.el e-chat-test-configures-evil-initial-state-as-emacs e-chat-test-overview-mode-neutralizes-evil e-chat-test-evil-local-mode-hook-disables-reactivation e-chat-test-overview-renders-sessions-in-recency-order e-chat-test-overview-compacts-multiline-session-summary e-chat-test-overview-styles-session-row-regions e-chat-test-overview-hides-summary-when-title-is-derived e-chat-test-overview-j-k-move-by-session-and-preview e-chat-test-overview-open-session-marks-session-read e-chat-test-sidebar-toggle-opens-and-closes-overview e-chat-test-shell-descriptor-advertises-chat-surface
rtk eldev test -f test/e-chat-test.el
#+end_src

Result:

- targeted overview/shell tests: 3 expected, 0 unexpected
- targeted overview/sidebar polish tests: 8 expected, 0 unexpected
- targeted overview/sidebar modal tests: 11 expected, 0 unexpected
- full chat shell test file: 147 expected, 1 skipped, 0 unexpected

Live Emacs:

- The normal =e-dev-reload= command stalled again and the stuck =emacsclient=
  process was stopped.
- Narrowly loaded =lisp/shells/chat/e-chat.el= into the running Emacs with
  =load-file=.
- A temporary live harness probe verified newest-first overview ordering,
  opening the selected session, persisting the latest assistant read marker, and
  clearing the unread marker after open.
- After sidebar row styling, a live =*e-chat-overview*= refresh showed compact
  multi-line rows with separate title, metadata, and summary faces.
- After sidebar interaction fixes, a live =*e-chat-overview*= probe confirmed
  no row-wide =mouse-face=, =j= moved to the next session id, and the preview
  buffer was live.
- After overview modal fixes, sidebar buffers disable local Evil state, =RET=,
  =j=, and =k= resolve to overview commands, and
  =e-chat-sidebar-toggle= selects the sidebar window when opening it.
- The latest modal-fix pass reloaded successfully with =e-dev-reload=; a live
  key macro probe confirmed =j= moved from the first session row to the next
  session row and left the overview selected with a live preview buffer.

* Review Notes

Unread detection avoids transcript loads during listing. For sessions that are
already loaded, it tracks the latest assistant message id or timestamp. For
index-only historical sessions, no unread marker is inferred until opening or
another path loads the transcript.
