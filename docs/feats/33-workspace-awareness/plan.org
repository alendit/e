#+title: Workspace Awareness
#+startup: overview

* Status

In-review.

* Goal

Teach =e= to respect editor workspaces as a first-class interaction boundary,
with one Org canvas per workspace as a supported workflow.

The immediate failures to address are:

- Org canvas chat/input/result buffers sometimes appear in the wrong workspace
  for the wrong canvas.
- When an agent focuses a buffer, it can open or select that buffer in another
  workspace, especially when that other workspace is currently selected or when
  a display helper reuses a window globally.
- The model often manipulates windows through arbitrary =run_elisp= calls, so a
  solution that relies only on the model remembering a custom display action will
  be unreliable.

Deliver a =workspace-awareness= capability plus supporting presentation/runtime
APIs so presentation shells have workspace affinity, =e='s own shells route
displays to the right workspace, and agent-written Elisp has clear actions and
skill guidance for workspace-aware visibility changes.

* Research notes

** Emacs display-buffer gives useful but incomplete primitives

The Emacs Lisp manual documents =display-buffer= action alist entries that can
limit where a buffer is shown:

- =reusable-frames= controls which frames are searched when reusing a window
  that already displays the buffer.  =nil= means the selected frame, while =t=
  means all frames and is rarely the right default.
- =lru-frames= similarly scopes which frames are searched when using some
  existing window.
- =frame-predicate= can restrict frame candidates.
- =inhibit-switch-frame= prevents raising/selecting another frame when display
  chooses one.

This is valuable for =display-buffer=, =pop-to-buffer=, and presentation helpers
that can be routed through =display-buffer=.  It is not a complete solution
because agent Elisp can call =switch-to-buffer=, =select-window=,
=set-window-buffer=, or package-specific workspace/window functions directly.

Source: GNU Emacs Lisp Reference Manual, "Action Alists for Buffer Display" and
"Action Functions for Buffer Display".

** Doom workspaces are adapter-friendly

Doom's =:ui workspaces= module is powered by =persp-mode= and documents a
workspace API such as =+workspace-current=, =+workspace-current-name=,
=+workspace-switch=, =+workspace-buffer-list=, and =+workspace-exists-p=.  It
also notes that with =persp-mode= active, workspace buffer lists are restricted,
which matches the desired mental model for canvas-specific buffers.

This suggests =e= should not hardcode Doom, but it can support Doom well through
an adapter.  The same adapter contract can later support builtin =tab-bar=,
=perspective.el=, =tabspaces=, or a single-workspace fallback.

Source: Doom Emacs =:ui workspaces= module documentation.

* Current behavior and likely failure points

Relevant existing surfaces:

- =lisp/shells/e-org-canvas.el= creates and displays Org canvas sessions and
  input/result panes.  It has several direct display/focus calls:
  =switch-to-buffer=, =pop-to-buffer=, =display-buffer=, =set-window-buffer=,
  and =select-window=.
- =lisp/shells/chat/e-chat.el= wraps some chat display operations with
  =e-chat--switch-to-buffer= and =e-chat--pop-to-buffer=, but those helpers are
  side-window aware, not workspace aware.
- =lisp/layers/emacs/e-emacs-tools.el= exposes =run_elisp=.  The evaluated code
  currently has no workspace-specific shell guidance by default; if it focuses a
  buffer with raw display functions, Emacs and the user's workspace package
  decide where it appears.
- =lisp/layers/org-canvas/e-org-canvas-capabilities.el= contributes strong
  canvas-specific context and visibility tools, but no workspace context.

The bug is therefore not one thing.  It is a mix of shell display routing,
canvas/chat presentation identity, and arbitrary Elisp side effects during agent
turns.

* Options considered

** Option A: Static guidance in context only

Add instructions such as "respect the current workspace; do not display buffers
in another workspace" to an Emacs or Org Canvas context provider.

Pros:

- Very cheap and easy to ship.
- No new actions and no runtime coupling to workspace packages.
- Helps when the model writes explicit Elisp and remembers the instruction.

Cons:

- Too weak for a first-class workflow.  The model can miss it, especially when
  using standard functions it assumes are harmless.
- Does not fix =e='s own presentation shell display calls.
- Does not protect direct =run_elisp= calls that use =switch-to-buffer= or
  =pop-to-buffer= out of habit.

Verdict: useful as one layer, insufficient alone.

** Option B: Documentation/skill for standard window functions

Add a skill or reference documenting workspace-safe usage of
=display-buffer=, =pop-to-buffer=, =switch-to-buffer=, =set-window-buffer=, and
workspace-manager APIs.

Pros:

- Better detail than a short system instruction.
- Can explain pitfalls like =reusable-frames t= and cross-frame/window reuse.
- Useful for design/debugging tasks.

Cons:

- Still pull-based.  The model may not read it before a quick focus action.
- The model may assume standard Emacs functions need no special docs.
- Does not correct =e='s own shell behavior.

Verdict: valuable supporting material, not the reliability mechanism.

** Option C: Window manipulation capability with dedicated actions

Expose model-facing actions such as =workspace_state=, =workspace_focus_buffer=,
=workspace_show_shell=, and perhaps =workspace_display_buffer= that wrap standard
window functions and run hooks/adapters.

Pros:

- Most reliable when used: the action can inspect the shell/canvas workspace,
  switch or validate the workspace, add buffers to the right workspace, and
  report what it did.
- Keeps workspace policy in one component instead of scattered Elisp snippets.
- Good user-facing affordance: the model has an obvious way to focus/show
  things.

Cons:

- Risk of action explosion if every window operation gets an action.
- Extra action round trips can slow interaction.
- The model may still bypass the actions with =run_elisp= for familiar functions.
- Needs gradual discoverability and naming discipline.

Verdict: include a very small high-value action set, but do not rely on
actions as the only guardrail.

** Option D: Presentation-side workspace affinity

Capture a workspace token when a chat or Org canvas presentation surface is
created, store it in shell-owned and buffer-local state, and make =e='s own
shell display helpers route buffers through a workspace-aware display service.

Pros:

- Fixes the highest-value first-party bugs without model cooperation.
- Gives a durable presentation identity: canvas/chat surface X belongs to
  workspace Y; canvas buffer and input/result buffers inherit that affinity.
- Keeps presentation behavior in presentation shells rather than harness core.
- Can preserve the user's current workspace when passive rendering occurs.

Cons:

- Does not constrain arbitrary =run_elisp= unless combined with another layer.
- Requires careful adapter design for multiple workspace packages.
- Existing direct =pop-to-buffer= / =switch-to-buffer= / =set-window-buffer=
  calls must be migrated.

Verdict: required core slice.

** Option E: Skill guidance for agent Elisp

When a shell/canvas surface has workspace affinity, give the model concise
guidance and actions that make workspace-safe display the obvious path for
agent-written Elisp without intercepting arbitrary Emacs functions.

The guidance can combine:

- Capability context describing the active shell/canvas workspace.
- Action descriptions for =workspace_state=, =workspace_focus_buffer=, and
  =workspace_show_shell=.
- A workspace-aware display reference for direct Elisp.
- First-party shell display helpers that already route =e=-owned buffers through
  the workspace service.

Pros:

- Low model burden for common operations: use actions first, helpers second.
- Avoids surprising dynamic advice around standard Emacs functions.
- Keeps ownership in presentation shells and capability guidance.

Cons:

- Weaker than enforcement for arbitrary raw Elisp.
- Requires action descriptions and context to stay concise and current.
- Does not constrain package-specific workspace/window internals.

Verdict: ship this instead of an ambient guard for the first slice; revisit
enforcement only if action/helper guidance proves insufficient.

** Option F: Global display-buffer-alist policy only

Install =display-buffer-alist= rules for =e= chat/canvas buffers so they prefer
or require the current workspace/frame.

Pros:

- Uses standard Emacs display mechanisms.
- Helps with any package code that uses =display-buffer=.
- Can prevent obvious cross-frame reuse by controlling =reusable-frames= and
  =lru-frames=.

Cons:

- Global display rules can fight user configuration and popup managers.
- Does not cover =switch-to-buffer=, =select-window=, or direct
  =set-window-buffer=.
- Workspace semantics differ across Doom/persp/tab-bar; =display-buffer= knows
  frames and windows, not perspectives.

Verdict: use as an implementation detail inside =e-workspace-display-buffer= and
  workspace-aware helpers, not as the whole feature.

* Chosen design

Use a presentation-owned layered approach:

1. Add a workspace backend abstraction and a small =workspace-awareness=
   capability.
2. Make chat and Org canvas shells record workspace affinity in shell/buffer
   state, not in core session records.
3. Route =e='s own display/focus operations through a workspace-aware display
   service.
4. Contribute concise dynamic context plus a tiny action set with clear
   descriptions for common workspace operations.
5. Add skill/reference guidance for agent-written Elisp so raw visibility code
   prefers the workspace-aware shell actions/helpers.

This keeps workspace ownership in presentation shells and avoids a large
window-action surface or dynamic advice around arbitrary Elisp.

* User-facing behavior

- Opening a chat or Org canvas presentation surface captures the current
  workspace as that shell/canvas surface's workspace affinity.
- Reopening the session from another workspace either:
  - switches to the shell/canvas workspace and shows it there, or
  - when a command explicitly requests "show here", rebinds the session to the
    current workspace after confirmation/configuration.
- Org canvas input/result buffers appear in the same workspace and adjacent to
  their target canvas window when possible.
- If one workspace per canvas is used, =e= keeps each canvas, its transient input
  pane, and its backing chat buffer associated with that workspace.
- During an agent turn, focus/display operations prefer the active shell/canvas
  workspace.
  If an operation would show a buffer in another workspace, =e= either routes it
  back, switches to the right workspace first, or reports a warning in action result/context depending on the operation and configuration.
- Users can intentionally request cross-workspace operations when they are doing
  workspace management.

* Architecture

** Workspace backend contract

Add a new presentation/runtime support module, tentatively
=lisp/shells/e-workspaces.el= or =lisp/layers/emacs/e-workspace-awareness.el=,
with a narrow adapter contract:

#+begin_src elisp
(cl-defstruct e-workspace-token
  backend       ;; 'doom | 'persp | 'tab-bar | 'single | ...
  id            ;; stable value when available
  name          ;; readable workspace name
  frame)        ;; frame this token was captured on, when relevant

(defun e-workspace-current (&optional frame) ...)
(defun e-workspace-equal-p (a b) ...)
(defun e-workspace-live-p (token) ...)
(defun e-workspace-switch (token) ...)
(defun e-workspace-buffer-member-p (buffer token) ...)
(defun e-workspace-add-buffer (buffer token) ...)
(defun e-workspace-visible-window (buffer token) ...)
#+end_src

Backends, in priority order:

1. Doom workspaces when =+workspace-current= / related APIs are available.
2. =persp-mode= directly when Doom wrappers are unavailable.
3. Builtin =tab-bar= tabs.
4. Single-workspace fallback, where all operations are no-ops and every buffer
   belongs to the one implicit workspace.

The contract is intentionally small.  It does not try to model every workspace
package feature; it only answers what =e= needs: current workspace, switch/show,
buffer membership, and visibility.

** Shell and buffer affinity

Workspace affinity is presentation state.  Do not store workspace metadata in
core session records or add =e-session-workspace= helpers to the harness/session
service.  Instead, chat and Org canvas shells capture the current workspace token
when they create or display a presentation surface and keep it in shell-owned
state:

#+begin_src elisp
:workspace (:backend doom :id ... :name "research" :frame ...)
#+end_src

For Org canvas surfaces, keep this alongside existing shell-side Org canvas
state and set buffer-local affinity on:

- the Org canvas buffer,
- the e chat buffer attached to the canvas surface,
- transient =*e-org-canvas-input:...*= buffers,
- any future canvas-specific helper buffers.

Add presentation helpers:

#+begin_src elisp
(e-shell-workspace shell-buffer-or-state)
(e-shell-set-workspace shell-buffer-or-state token)
(e-buffer-workspace buffer)
(e-buffer-set-workspace buffer token)
(e-org-canvas-workspace buffer-or-state)
(e-chat-buffer-workspace buffer)
#+end_src

Ownership: workspace affinity belongs to presentation shells and buffer-local
state.  The harness/session service remains workspace-agnostic; backend package
details stay in the workspace adapter module.

** Workspace-aware display service

Introduce a small set of internal helpers for presentation code:

#+begin_src elisp
(e-workspace-display-buffer buffer &key workspace action select side-window-ok)
(e-workspace-pop-to-buffer buffer &key workspace)
(e-workspace-switch-to-buffer buffer &key workspace)
(e-workspace-select-buffer-window buffer &key workspace)
#+end_src

Behavior:

- If BUFFER is already visible in WORKSPACE, select/reuse that window.
- Otherwise ensure BUFFER is a member of WORKSPACE when the backend supports
  membership, switch to WORKSPACE if necessary, and display it on the selected
  frame/window using =display-buffer= with action entries that avoid unrelated
  frames (=reusable-frames nil=, =lru-frames nil=, =inhibit-switch-frame t=,
  and a =frame-predicate= where applicable).
- Preserve side-window handling already present in =e-chat--display-from-side-window=.
- Keep user display customization in play where safe, but do not let it move
  =e= canvas/chat buffers to another workspace.

Migrate direct first-party display/focus calls in =e-chat.el= and
=e-org-canvas.el= onto these helpers.

** Capability context

Add =e-workspace-awareness-capability-create= contributing dynamic context when
workspace support is active:

- current workspace token/name,
- active shell/canvas workspace token/name,
- whether the selected workspace matches the active shell/canvas workspace,
- visible =e= chat/canvas/input buffers grouped by workspace when the backend
  can answer that,
- concise instruction: prefer workspace actions/display helpers over raw
  =switch-to-buffer=; if writing Elisp that changes visibility, use the
  workspace-aware display helpers or shell action.

This is a signal, not the only enforcement mechanism.

** Small model-facing action set

Avoid an action for every window primitive.  Expose only high-value shell actions
with clear descriptions and capability guidance:

1. =workspace_state= :: report current/shell workspace, visible =e= buffers, and
   mismatches.
2. =workspace_focus_buffer= :: focus a named buffer in the shell workspace,
   optionally adding it to that workspace.
3. =workspace_show_shell= :: show the current chat/canvas buffers in their shell
   workspace.

These actions are discoverable, reliable, and should cover the common "focus
something" use case without exploding the surface area.  Prefer good action
descriptions and skill guidance over a separate tool taxonomy.

** Skill guidance for agent Elisp

Do not add an ambient =run_elisp= guard in the first slice.  Dynamic advice around
arbitrary Elisp is too surprising for a presentation concern and makes ownership
unclear.  Instead, ship capability context and skill/reference guidance that
points model-written Elisp at the workspace-aware shell actions/helpers:

#+begin_src elisp
(e-workspace-display-buffer buffer :workspace workspace)
(e-workspace-pop-to-buffer buffer :workspace workspace)
(e-workspace-switch-to-buffer buffer :workspace workspace)
#+end_src

Guidance responsibilities:

- Identify the active shell/canvas workspace from buffer-local or shell state,
  falling back to the current workspace when no shell affinity exists.
- Prefer =workspace_state=, =workspace_focus_buffer=, and =workspace_show_shell=
  for common focus/show operations.
- When writing Elisp directly, use the workspace-aware display helpers instead
  of raw =switch-to-buffer= / =pop-to-buffer= / =display-buffer=.
- Keep intentional cross-workspace administration explicit in the prompt or
  command, rather than hidden behind dynamic advice.

This answers the "do not add a hook/context insert for every Elisp call" concern
without dynamic advice: the common operations are available as described actions,
and lower-level Elisp guidance points to the same workspace-aware display
helpers.

* Configuration

Add defcustoms under a new =e-workspace= group:

- =e-workspace-awareness-enabled= (default t when the capability is active).
- =e-workspace-awareness-backend-priority= (default Doom, persp, tab-bar,
  single).
- =e-workspace-display-cross-workspace-policy=:
  - =switch= (default for explicit open/focus commands),
  - =route= (show in shell/canvas workspace without permanently changing
    affinity),
  - =warn=,
  - =allow=.
- =e-workspace-rebind-shell-on-open= (default nil; explicit command can rebind
  a chat/canvas presentation surface).

* Implementation plan

** Task 1: Workspace backend abstraction

Files:

- Add =lisp/shells/e-workspaces.el= or =lisp/layers/emacs/e-workspaces.el=.
- Add =test/e-workspaces-test.el=.

Steps:

1. Define =e-workspace-token= and token comparison/formatting helpers.
2. Implement the single-workspace fallback.
3. Implement backend detection and adapter dispatch.
4. Add Doom/persp adapter functions guarded by =fboundp= / =featurep= checks.
5. Add tab-bar adapter functions guarded by =fboundp= checks.
6. Test fallback behavior and adapter dispatch by stubbing Doom/persp/tab-bar
   functions.

Verification:

#+begin_src sh
rtk eldev test -f e-workspaces-test
#+end_src

** Task 2: Shell and buffer workspace affinity

Files:

- Modify =lisp/shells/chat/e-chat.el=.
- Modify =lisp/shells/e-org-canvas.el=.
- Add/modify tests in =test/e-chat-test.el= and =test/e-org-canvas-test.el=.

Steps:

1. Capture =e-workspace-current= when chat/canvas shells create or display
   presentation surfaces, and store it in shell-owned or buffer-local state.
2. Mark chat buffers, Org canvas buffers, and Org canvas input/result buffers
   with buffer-local workspace affinity.
3. Preserve existing shell-owned workspace affinity when Org canvas metadata is
   refreshed.
4. Add a command/helper to explicitly rebind a shell/canvas surface to the
   current workspace.
5. Test that creating/resuming Org canvas surfaces stores and preserves the
   expected token without changing core session records.

Verification:

#+begin_src sh
rtk eldev test -f e-chat-test -f e-org-canvas-test -f e-workspaces-test
#+end_src

** Task 3: Workspace-aware display service and shell migration

Files:

- Modify =lisp/shells/e-workspaces.el=.
- Modify =lisp/shells/chat/e-chat.el=.
- Modify =lisp/shells/e-org-canvas.el=.
- Modify =test/e-chat-test.el= and =test/e-org-canvas-test.el=.

Steps:

1. Implement =e-workspace-display-buffer=, =e-workspace-pop-to-buffer=,
   =e-workspace-switch-to-buffer=, and =e-workspace-select-buffer-window=.
2. Route chat display helpers through these functions.
3. Route Org canvas buffer selection, input pane display, input focus return,
   and backing-chat hiding through workspace-aware helpers.
4. Ensure side-window-safe behavior is preserved.
5. Test: with a stub backend, opening canvas A while workspace B is selected
   switches/routes to A's workspace instead of showing A in B; two visible
   canvases keep their input panes below their own canvas windows.

Verification:

#+begin_src sh
rtk eldev test -f e-chat-test -f e-org-canvas-test -f e-workspaces-test
#+end_src

** Task 4: Workspace-awareness capability context and actions

Files:

- Add =lisp/layers/emacs/e-workspace-capabilities.el= or extend
  =e-emacs-capabilities.el= if kept small.
- Modify =lisp/layers/emacs/e-emacs-base.el= or add a separate layer spec in
  =lisp/defaults/e-default-layers.el=.
- Add tests.

Steps:

1. Add context provider with current/shell workspace and mismatch summary.
2. Register actions =workspace_state=, =workspace_focus_buffer=, and
   =workspace_show_shell=.
3. Add the capability to =emacs-base= or a new default layer
   =workspace-awareness=.  Prefer a separate capability id even if packaged in
   =emacs-base= so users can configure/disable it.
4. Test model-facing context and action behavior with stubbed backends.

Verification:

#+begin_src sh
rtk eldev test -f e-workspaces-test -f e-chat-test -f e-org-canvas-test
#+end_src

** Task 5: Workspace guidance for agent Elisp

Files:

- Add a workspace-awareness skill/reference resource.
- Extend capability context/action descriptions in the workspace-awareness
  capability.
- Add tests where context/action registration is testable.

Steps:

1. Add concise guidance for model-written Elisp: inspect workspace state first,
   then use workspace actions or workspace-aware display helpers for visibility
   changes.
2. Ensure action descriptions explain when to use =workspace_state=,
   =workspace_focus_buffer=, and =workspace_show_shell=.
3. Ensure the context names shell/canvas affinity as presentation state and does
   not imply core session metadata owns workspaces.
4. Document intentional cross-workspace administration as an explicit opt-in
   request, not an ambient default.

Verification:

#+begin_src sh
rtk eldev test -f e-workspaces-test
#+end_src

** Task 6: Live verification in running Emacs

Steps:

1. Reload changed Elisp:

#+begin_src sh
emacsclient --eval "(progn (require 'e-dev) (e-dev-reload \"/Users/dimitrivorona/projects/elisp/e\"))"
#+end_src

2. In Doom workspaces, create two workspaces with one Org canvas each.
3. Start one prompt from each canvas and confirm the transient input/result pane
   appears with its own canvas.
4. From workspace B, resume/focus workspace A's canvas surface and confirm =e=
   switches/routes to A instead of showing A's buffers in B.
5. Ask the agent to focus a known buffer through the workspace action or display
   helper and confirm it stays inside the active shell/canvas workspace.

* Acceptance criteria

- Chat and Org canvas shells record workspace affinity at creation/display.
- Org canvas buffers, input/result buffers, and backing chat buffers retain the
  same workspace affinity.
- First-party =e= display/focus calls route through workspace-aware helpers; the
  known wrong-workspace canvas/chat display bug is covered by tests.
- =workspace-awareness= context tells the model the current/shell workspace and
  warns on mismatch.
- A small action set lets the model inspect workspace state and focus buffers in
  the shell workspace without using raw Elisp.
- Model-written Elisp guidance points to workspace-aware display helpers instead
  of an ambient dynamic advice guard.
- Doom/persp and tab-bar support are adapter-based and optional; Emacs without a
  workspace package falls back to a no-op single workspace.

* Risks and mitigations

- *Workspace package APIs differ.*  Keep the adapter contract tiny and test with
  stubbed backends.
- *Raw model-written Elisp may still bypass workspace helpers.*  Keep capability
  guidance concise, make the action descriptions obvious, and route first-party
  shell display paths through workspace-aware helpers so the common path is safe
  without dynamic advice.
- *User display-buffer customization may conflict.*  Route only =e=-owned
  buffers strictly; for arbitrary buffers prefer same-workspace display but
  report when user configuration overrode it.
- *Switching workspaces as a side effect can be surprising.*  Distinguish
  explicit focus/open commands (allowed to switch) from passive rendering
  (should route/preserve).  Make policy configurable.
- *Shell/canvas surface moved intentionally to another workspace.*  Provide explicit rebind
  command and do not silently rewrite affinity on every open.

* Test plan

Focused unit tests:

#+begin_src sh
rtk eldev test -f e-workspaces-test
#+end_src

Shell integration tests:

#+begin_src sh
rtk eldev test -f e-chat-test -f e-org-canvas-test -f e-workspaces-test
#+end_src

Capability guidance tests:

#+begin_src sh
rtk eldev test -f e-workspaces-test
#+end_src

Full relevant suite before review:

#+begin_src sh
rtk eldev test -f e-chat-test -f e-org-canvas-test -f e-workspaces-test
#+end_src

Live verification is required because workspace/window behavior depends on the
running Emacs, Doom/persp/tab-bar state, and actual windows.

* Architecture review questions

1. If this work package stopped here, would the project be better off than if it
   had not been built?  Yes: one-canvas-per-workspace becomes a supported,
   tested interaction path instead of accidental behavior.
2. What final direction does this move toward?  =e= as an Emacs-native agent
   that understands editor presentation state, not only files and buffers.
3. Which decisions are likely to change, uncertain, or stable?  Stable: shell/canvas surfaces
   need workspace affinity and first-party displays should respect it.
   Likely-to-change: exact backend APIs and guidance/action wording; both are
   local to adapters/config/capability descriptions.
4. What component owns this behavior, and why?  Workspace adapters own package
   integration; shells own buffer display; the capability owns model context, actions, and guidance.
5. Does this change increase or reduce coupling?  It reduces scattered coupling
   by routing display through one workspace service; adapters isolate Doom/persp
   and tab-bar details.
6. Did any dependency start pointing the wrong way?  The plan keeps workspace
   package dependencies at the presentation/Emacs edge, not in backend/provider
   core.
7. Could any side effect be moved outward into an adapter or shell?  Yes, and
   the plan does that: switching/showing workspaces is adapter/shell behavior;
   core stores only metadata if needed.
8. Are the abstractions semantically real?  Yes: current workspace, shell/canvas
   workspace, buffer membership, and display-in-workspace are real operations
   shared across Doom/persp/tab-bar even though implementation details differ.
9. What compatibility expectations apply?  Existing no-workspace Emacs must keep
   working through the single-workspace fallback.  Existing shells/canvases without
   workspace metadata use current workspace and can be backfilled lazily.
10. What legacy code can we remove now?  After migration, direct =e=-owned
    =pop-to-buffer= / =switch-to-buffer= helpers that only handle side windows
    can be folded into the workspace display service.
11. Where are expected errors handled, and where do unexpected errors surface?
    Missing workspace package support falls back to single workspace; missing or
    deleted workspace tokens become user-visible warnings.  Unexpected adapter
    errors surface during tests/live verification rather than being swallowed.
12. What tests prove the core behavior independently of the full system?
    Stubbed backend tests prove token comparison, membership, display routing,
    and routing behavior without relying on a real Doom/persp/tab-bar workspace.
