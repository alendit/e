#+title: Review: Message and Turn Identity
#+startup: overview

* Scope

Validated the current repo implementation against =plan.org= for feature 04.

Inspected directly or by outline/search:

- =lisp/core/e-session.el=
- =lisp/core/e-harness.el=
- =lisp/core/e-loop.el=
- =lisp/core/e-context.el=
- =lisp/core/e-compaction.el=
- =lisp/core/e-tools.el=
- =lisp/shells/chat/e-chat.el= turn/replay touch points
- =test/e-session-test.el=
- =test/e-harness-test.el=
- =test/e-loop-test.el=
- =test/e-context-test.el=
- =test/e-compaction-test.el=
- broader chat/session tests through the full test suite

* Verification

Commands run for this review pass:

#+begin_src shell
eldev test
#+end_src

Result: 452 tests run; 451 results as expected; 0 unexpected; 1 skipped (=e-chat-test-assistant-markdown-renders-with-text-properties=).

Live reload:

#+begin_src shell
emacsclient --eval "(progn (require 'e-dev) (e-dev-reload \"/Users/dimitrivorona/projects/elisp/e\"))"
#+end_src

Result: returned ="/Users/dimitrivorona/projects/elisp/e/"=.

Live harness turn probe after reload:

#+begin_src elisp
(progn
  (require 'e)
  (require 'e-session)
  (require 'e-harness)
  (let* ((store (e-session-store-create))
         (harness (e-harness-create
                   :sessions store
                   :backend (e-backend-fake-create
                             :items '((:type reasoning-delta :content "thinking")
                                      (:type assistant-message :content "ok")
                                      (:type done :reason stop))))))
    (e-harness-create-session harness :id "codex-review-04b")
    (let ((turn-id (e-harness-prompt-async harness "codex-review-04b" "live review probe")))
      (e-harness-wait harness "codex-review-04b" 1.0)
      (let* ((messages (e-session-messages store "codex-review-04b"))
             (events (e-session-activity-events store "codex-review-04b"))
             (path (e-session-current-path store "codex-review-04b")))
        (list :turn-id-ulid (and (stringp turn-id)
                                 (not (null (string-match-p "\\`[0-9A-HJKMNP-TV-Z]\\{26\\}\\'" turn-id))))
              :message-count (length messages)
              :event-types (mapcar (lambda (e) (plist-get e :event-type)) events)
              :all-turns-match (cl-every (lambda (entry)
                                           (equal (plist-get entry :turn-id) turn-id))
                                         (append messages events))
              :path-types (mapcar (lambda (e) (plist-get e :type)) path)
              :parents-ok (cl-loop for prev = nil then entry
                                   for entry in path
                                   always (if prev
                                              (equal (plist-get entry :parent-id)
                                                     (plist-get prev :id))
                                            (not (plist-get entry :parent-id))))))))
#+end_src

Result:

#+begin_src elisp
(:turn-id-ulid t
 :message-count 2
 :event-types (turn-started provider-request-started reasoning-delta provider-request-finished turn-finished)
 :all-turns-match t
 :path-types (message activity-event activity-event activity-event activity-event message activity-event)
 :parents-ok t)
#+end_src

Live reset/head probe after reload:

#+begin_src elisp
(progn
  (require 'e-session)
  (let ((store (e-session-store-create)))
    (e-session-create store :id "reset-probe")
    (let ((first (e-session-append-message store "reset-probe" '(:role user :content "before"))))
      (e-session-clear-messages store "reset-probe")
      (let ((after (e-session-append-message store "reset-probe" '(:role user :content "after"))))
        (list :after-parent (plist-get after :parent-id)
              :old-id (plist-get first :id)
              :path-ids (mapcar (lambda (e) (plist-get e :id))
                                 (e-session-current-path store "reset-probe")))))))
#+end_src

Result:

#+begin_src elisp
(:after-parent "01KSF8BSVX4S2MBT3AJ6N3MNNQ"
 :old-id "01KSF8BSVX4S2MBT3AJ6N3MNNQ"
 :path-ids ("01KSF8BSY09GF9ZWQ191Y3RZ3M"))
#+end_src

This exposes a dangling parent after a live clear/reset; see [[*Findings]].

* Resolution Verification

Commands run after addressing the findings:

#+begin_src shell
eldev test test/e-session-test.el
eldev test test/e-session-test.el test/e-harness-test.el test/e-chat-test.el test/e-context-test.el test/e-compaction-test.el
eldev test
#+end_src

Results:

- =test/e-session-test.el=: 30 tests run; 30 results as expected; 0 unexpected.
- Focused session/harness/chat/context/compaction suite: 202 tests run; 201 results as expected; 0 unexpected; 1 skipped (=e-chat-test-assistant-markdown-renders-with-text-properties=).
- Full suite: 446 tests run; 445 results as expected; 0 unexpected; 1 skipped (=e-chat-test-assistant-markdown-renders-with-text-properties=).

Live reload:

#+begin_src shell
emacsclient --eval "(progn (require 'e-dev) (e-dev-reload \"/Users/dimitrivorona/projects/elisp/e\"))"
#+end_src

Result: returned ="/Users/dimitrivorona/projects/elisp/e/"=.

Live harness reset-path probe after reload:

#+begin_src elisp
(:clear-type messages-cleared
 :clear-parent-is-root t
 :new-parent-is-clear t
 :path-markers (session-created messages-cleared user)
 :path-valid t)
#+end_src

* Acceptance Review

| Plan criterion | Review |
|----------------+--------|
| Every new durable session entry has an =id=. | Satisfied. Messages, activity events, branch summaries, compactions, and session-event records all have ids. =session=, =session-info=, =current-branch=, and =messages-cleared= are now eventified and queryable. |
| Every new durable session entry has a =parent-id= except the root entry. | Satisfied. The =session-created= root has no parent. Messages and state events parent to the current head, and =messages-cleared= intentionally parents to the session root before advancing the head. |
| Every assistant/user operation has a shared =turn-id=. | Satisfied for harness-owned turns. =e-harness--next-turn-id= allocates a ULID, and the harness tags the user message, assistant/tool messages, durable activity events, tool lifecycle context, resources, and context providers with the same turn id. Low-level =e-loop= intentionally delegates message tagging to its append callback. |
| Existing sessions can still be loaded and replayed. | Satisfied. Persistent replay preserves ids and parents when present. Legacy records without ids are backfilled in memory with deterministic ids and linear parent links. Full chat/session replay tests pass. |
| Session entries can be queried by id. | Satisfied through =e-session-entry-by-id= for messages, activity events, branch summaries, compactions, and eventified session records. |
| The current linear path can be reconstructed from parent ids. | Satisfied for normal append/replay paths, mixed message/activity paths, state-event paths, reset-boundary paths, and explicit synthetic branch heads. |
| The model is ready to store a future compaction entry with =first-kept-entry-id=. | Satisfied, and now beyond placeholder-only readiness. =e-session-append-compaction= persists =:first-kept-entry-id=, =e-session-latest-valid-compaction= validates current-path boundaries, =e-session-entries-from= computes the kept suffix, and =e-context= uses the summary plus suffix when building backend messages. |

* Findings

** Resolved: live clear/reset leaves a dangling parent head

=e-session-clear-messages= clears =:messages= and =:activity-events= in live state but does not clear =:current-head-id=. The replay handler for =messages-cleared= does clear =:current-head-id=, so live state and the intended reset semantics diverge.

Impact:

- The first entry appended after a live reset receives =:parent-id= equal to the id of an entry that was removed from the session entry collections.
- The entry behaves like a path root because =e-session-current-path= cannot resolve the old parent, but it is not represented as a root: its =:parent-id= is non-nil.
- Persistent append after reset writes the dangling =parent-id=, and replay preserves it because the parent id is stored on the message record.
- This weakens the feature's parent invariant and future branching/compaction assumptions that parent ids reference reachable durable entries.

Implemented fix:

#+begin_src elisp
(defun e-session-clear-messages (store session-id)
  ...
  (plist-put session :messages nil)
  (plist-put session :activity-events nil)
  (plist-put session :current-head-id root-id)
  (e-session--append-session-event
   session 'messages-cleared timestamp (list :parent-id root-id))
  ...)
#+end_src

Add a regression test that clears a session, appends a new message, and asserts:

- the clear event parents to =session-created=;
- the new message parents to =messages-cleared=;
- live and persistent replay preserve that reset-boundary path shape.

Resolution:

- =messages-cleared= is now a first-class =session-event= entry with an =id=.
- The clear event intentionally parents to the =session-created= root event instead of the removed transcript head.
- After clear, the session head advances to the clear event, so the next message parents to =messages-cleared=.
- Regression coverage now asserts the live and replayed reset path shape:
  =session-created -> messages-cleared -> user=.

** Resolved: audit non-entry JSONL records as event-log scope, not identity exceptions

The implementation clearly treats messages, activity events, branch summaries, and compactions as durable entries. Other append-only JSONL records are written into the same session log but are not first-class identifiable entries:

- =session=
- =session-info=
- =current-branch=
- =messages-cleared=

The sharper design rule should be: if a record is durable inside the session transcript/event log, it should represent a meaningful historical session event; if it is merely cursor, UI, view, cache, or derived-index state, it should not live in that log.

Implications for the current records:

| Record | Review recommendation |
|--------+-----------------------|
| =session= | Treat as the historical root event, or explicitly keep it as file header metadata outside the event chain. If it stays in the event log, giving it an =id= would make the root invariant cleaner. |
| =messages-cleared= | Treat as a historical transcript mutation. It should not be an anonymous side effect; it should either become a first-class event with an =id= and intentional parent semantics, or the next message should become a true new root after the clear. |
| =session-info= | Split by meaning. User-visible/context-affecting changes such as explicit name, model, reasoning effort, or project-root changes are historical session events and should be identifiable if stored in the log. Pure cache/index/derived display metadata should move out of the transcript log. |
| =current-branch= | If this is a checkout cursor or current UI/view state, it should not be in the session transcript log. Store it in sidecar state instead. If it is intended as a historical branch operation, rename/model it as such and give it entry identity. |

For future tree navigation, session comparison, replay diagnostics, time travel, and branch-aware context construction, the clean target is a unified durable event log containing only historical events. Every event in that log should be addressable by id. Ephemeral cursor/UI/cache state should be stored separately rather than exempted from identity.

This makes the reset bug more significant: =messages-cleared= is a historical transcript mutation, so treating it as anonymous mutable state creates ambiguity around whether later entries should parent to the old head, parent to the clear event, or start a new root.

Resolution:

- =session= now becomes the root =session-created= session event, with an =id= and no =parent-id=.
- =session-info= records now become identifiable =session-event= entries and advance the head for name, metadata, and turn-option changes.
- =current-branch= records now become identifiable =session-event= entries and advance the head for branch cursor changes.
- =messages-cleared= records now become identifiable =session-event= entries with reset-boundary semantics.
- =e-session-session-events= exposes these eventified records as a read-only accessor.
- =e-session-entry-by-id= and =e-session-current-path= now include session events, so existing and legacy logs have queryable event roots and valid parent chains.

** Resolved: synthetic branch path is not directly covered

The query helpers support reconstructing a path from an explicit optional =head-id= when entries have parent links, but the current tests do not appear to include a focused synthetic branch case from the feature testing plan. This is lower priority than the reset bug because the minimal acceptance criteria only require the current linear path, but a small synthetic branch test would protect the future branching intent.

Resolution:

- Added a focused synthetic branch regression test with two child messages under the =session-created= root.
- The test validates explicit left/right head path reconstruction and the default current head path.

* Positive Notes

- ULID generation is compact, Crockford Base32, and monotonic for same-millisecond generation in the current process.
- Legacy backfill uses timestamp/session/type/ordinal-based deterministic ids, which keeps parent references stable across repeated loads of unchanged legacy files.
- Harness turn identity is consistently propagated through messages, durable activity events, context providers, tool lifecycle context, and resource/tool helpers.
- Context construction strips storage-only fields (=id=, =turn-id=, =type=, =parent-id=, =created-at=) before sending backend messages.
- Compaction integration already uses feature 04 ids for durable =first-kept-entry-id= boundaries and suffix reconstruction.
- The earlier unresolved broader replay concern is now addressed by the full =eldev test= run and live harness replay probe in this review pass.

* Notes To Resolve

None remain. Feature 04 is =Done=.
